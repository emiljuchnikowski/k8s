kind: Deployment
apiVersion: apps/v1
metadata:
  name: smartsoft-site
spec:
  replicas: 5
  selector:
    matchLabels:
      tier: frontend
  template:
    metadata:
      labels:
        tier: frontend
    spec:
      containers:
      - name: smartsoft-site
        image: smartsoft001/site